import { TodoCompletedStyle } from './todo-completed-style';

describe('TodoCompletedStyle', () => {
  it('should create an instance', () => {
    const directive = new TodoCompletedStyle();
    expect(directive).toBeTruthy();
  });
});
